import { MatrixConfig } from '../types';

export const matrixConfig: MatrixConfig = {
  fontSize: 16,
  chars: '01',
  speed: 80,
  opacity: 0.07,
  rainbowWidth: 0.2, // Width of the rainbow effect as a percentage of screen width
};
